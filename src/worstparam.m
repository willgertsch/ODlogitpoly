% find best parameter values given a design
% D-optimality
% xi: vector of design points(1:n/2) and weights (n/2+1:end)
% beta: 3x2 matrix with parameter bounds, i.e. [low, up;...]
function obj = worstparam(xi, beta, p)
    
    
    
end